# AI PR Review 助手 — 方案文档

> 题目：题三 · AI PR Review 助手
> 周期：2026-05-29 00:00 ~ 2026-05-31 23:59（72h）
> 仓库：`LittleChiu/ai-pr-reviewer`（待建）

## 一、第一性分析

### 1.1 用户真实痛点（开发者评审 PR 时遇到什么？）

- **认知负担**：大 PR 几百行 diff，看完一遍要 30+ 分钟，注意力难持续
- **上下文割裂**：diff 只展示"改了什么"，不展示"为什么这么改"和"影响哪里"
- **风险盲区**：人工最容易漏的是**间接影响**（删了一个看似没用的字段，下游服务挂了）
- **评审一致性**：团队规范是否被遵守、命名风格、测试覆盖，全靠 reviewer 记忆
- **疲劳**：每周看几十个 PR，重复劳动多，关键判断少

### 1.2 现有方案的不足

| 工具 | 短板 |
|---|---|
| GitHub Copilot Code Review | 闭源、贵、不可定制、对国内网络不友好 |
| CodeRabbit / Greptile | 收费、企业向，个人/小团队接入门槛高 |
| 手写 ChatGPT prompt | 没上下文、纯 diff 看，准确度低 |

### 1.3 我们要做什么样的产品

**一句话**：粘 PR URL → 一键得到一份"高于平均水平 reviewer"的评审报告。

**MVP 三大核心能力**（题目原文要求）：
1. **PR 变更总结**：用人话说清楚这个 PR 改了什么、为什么改、改在哪些层
2. **风险代码识别**：标记可能引入 bug / 性能 / 安全问题的代码片段
3. **Review 建议生成**：给出可立即采纳的修改建议（指向具体行）

**差异化亮点**（区别于通用 AI Review 工具）：
- ⭐ **跨文件上下文**：拉取 PR 涉及文件的完整内容（不止 diff），让模型看见"前后文"，降低误报
- ⭐ **分模块评审策略**：架构层 / 实现层 / 测试层 / 文档层用不同 prompt + 不同模型档位
- ⭐ **置信度显示**：每条建议带"高/中/低"置信度，减少对 reviewer 的干扰
- ⭐ **可解释性**：每条结论附"依据"，让 reviewer 能快速判断采纳 / 忽略

## 二、技术选型

### 2.1 模型策略（已锁定）

| 任务 | 模型 | 理由 |
|---|---|---|
| 主评审（推理型）| `deepseek-v4-pro-max` | 长上下文、强推理、价格合理 |
| 快速分类（如"该文件是否值得深审"）| `deepseek-v4-flash` | 便宜快，做粗筛 |
| 视觉补充（PR 描述含截图/架构图时）| `gemini-3.1-flash-lite` | 多模态 |
| 兜底备份 | `claude-sonnet-4-6` | 主路挂了切兜底 |

**调用方式**：OpenAI 兼容 SDK，`base_url=https://https://your-gateway.example.com/v1`，统一 `OPENAI_API_KEY`。

### 2.2 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | **FastAPI** + uv 管包 | Python 生态做 LLM 接入最顺，PyGithub 直接用 |
| 前端 | **Next.js 15 (App Router)** + Tailwind + shadcn/ui | 一键 Vercel 部署，UI 组件成熟 |
| 数据 | **SQLite + SQLModel** | 缓存 PR 分析结果，轻量、零运维 |
| 任务 | **同步调用 + 流式输出（SSE）** | 不引 Celery/Redis，KISS |
| 部署 | 后端：本地 + Cloudflare Tunnel；前端：Vercel | 后端有 GPU/可缓存，前端要 CDN |
| GitHub 接入 | **httpx + GitHub REST API**（不用 PyGithub）| PyGithub 同步阻塞，httpx 原生异步、可流式 |

### 2.3 关键设计决策

#### 决策 1：上下文获取深度
- **方案 A**：仅拉 diff（快、不准）
- **方案 B**：拉 diff + 改动文件全文（折中）⭐
- **方案 C**：克隆整个仓库做 AST 分析（准、慢、复杂）

**选 B**：72h 内 C 不现实；A 准确率太低。B 用 GitHub raw content API 直接拿文件，几十毫秒。

#### 决策 2：模型上下文如何组织
按 **"文件级 → 块级 → 行级"** 三层 prompt：
1. 第一轮：给整个 PR diff + 描述，让模型出"PR 总览 + 高风险文件清单"
2. 第二轮：对每个高风险文件，单独给完整文件内容 + 该文件的 diff，让模型给"块级评审"
3. 第三轮：聚合，输出最终报告

好处：避免一次塞太多 token、降低噪声、模型注意力更聚焦。

#### 决策 3：误报控制
- 每条建议必须带"依据引用"（指向哪几行 + 引用什么规则/模式）
- 置信度低于阈值的建议折叠到"次要建议"区
- 对"明显是风格 nitpick"的建议，单独归到"建议"而非"问题"

#### 决策 4：响应速度
- 流式 SSE：用户先看到"PR 总结"，再逐个文件出来 review
- 缓存：同一 commit SHA 的分析结果存 SQLite，避免重复消耗 token

## 三、架构

```
┌──────────────────┐         ┌────────────────────────────┐
│  Next.js Front   │ ─SSE→   │  FastAPI Backend           │
│  (Vercel)        │         │  ─ /api/review (POST)       │
│  - PR URL 输入   │ ←JSON─  │  ─ /api/review/{id} (GET)   │
│  - 实时流式渲染  │         │  ─ /api/health              │
└──────────────────┘         └─────┬──────────────────────┘
                                   │
                ┌──────────────────┼──────────────────────┐
                ▼                  ▼                       ▼
        ┌──────────────┐  ┌────────────────┐    ┌────────────────┐
        │ GitHub API   │  │ LLM Gateway    │    │ SQLite Cache   │
        │ (PR/files)   │  │ (your-gateway.example.com)  │    │ (results)      │
        └──────────────┘  └────────────────┘    └────────────────┘
```

## 四、里程碑（72h 拆解）

| 阶段 | 时段 | 产出 | PR |
|---|---|---|---|
| **D1 上午** | 5/29 00-12 | 仓库 / CI / 后端骨架 / GitHub API 拉 PR | PR #1 #2 |
| **D1 下午** | 5/29 12-24 | LLM 评审核心（最小 prompt + 一轮分析） | PR #3 |
| **D2 上午** | 5/30 00-12 | 前端骨架 / 串联 / 流式 SSE | PR #4 #5 |
| **D2 下午** | 5/30 12-24 | 三层 prompt / 缓存 / 置信度 | PR #6 #7 |
| **D3 上午** | 5/31 00-12 | 视觉模型补充 / UI 打磨 / 部署 | PR #8 #9 |
| **D3 下午** | 5/31 12-23 | README / Demo 视频脚本 / 收尾 | PR #10 |

> 实际节奏会根据进展自适应。每一步先小 PR 再迭代，禁止"末日大爆发"。

## 五、风险登记

| 风险 | 概率 | 缓解 |
|---|---|---|
| LLM 网关挂（已发生过 upstream_error）| 中 | 预设 fallback 链：deepseek→claude→gemini |
| GitHub API rate limit | 低 | 用 gh auth 的 token，5000/h 够用；缓存命中减少调用 |
| 大 PR token 爆炸 | 中 | 文件级筛选 + 截断 + 摘要降级 |
| 前端 Vercel 国内访问慢 | 低 | 备用：把前端也部署到本地 + Cloudflare Tunnel |
| 视觉模型在网关被价格未配置卡住 | 已发生 | 视觉部分做成可选 feature，不阻塞主路 |

## 六、成功标准（评分对照）

### 完整度 / 创新性（40%）
- ✅ 三大核心能力完整可用
- ✅ 三层 prompt + 跨文件上下文 = 比通用 ChatGPT prompt 强
- ✅ 置信度 + 可解释性 = 实际可用，不只是 demo

### 开发过程 / 质量（40%）
- ✅ 至少 10 个有意义的 PR，每个 PR 单一功能
- ✅ commit 时间分布覆盖 72h，不集中末日
- ✅ CI 跑 lint + typecheck + test，PR 必过
- ✅ README / PLAN / PROGRESS 三件套完整

### 演示 / 表达（20%）
- ✅ Demo 视频脚本：现场分析一个真实开源项目的 PR
- ✅ README 含技术选型 / 架构图 / 模型策略 / 未来扩展四节
